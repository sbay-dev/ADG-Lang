# Portal task fixtures

This directory keeps the sanitized source packet and the deterministic task
snapshot used by portal tests and release-integrity checks.

The authoritative catalog entry synchronized to the live portal is:

`human-evidence/tasks/natural-arabic-rule-consumption-v1.task.json`

Only root-level `human-evidence/tasks/*.task.json` files are read by the
repository task synchronization workflow.
